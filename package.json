{
  "name": "calendar-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "npx cap init \"Calendar App\" com.xueyujun.calendar --web-dir=dist",
    "mobile:build": "npm run build && npx cap copy",
    "mobile:open:ios": "npm run mobile:build && npx cap open ios",
    "mobile:open:android": "npm run mobile:build && npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.1",
    "@capacitor/ios": "^8.0.1",
    "date-fns": "^2.30.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
